// Local development only: OpenCode auto-loads TUI plugins from this directory.
// Published installs reach the same module through the package's "./tui" export.
export { default } from "../../../src/tui.js"
